function highReportCell(bgColor, fontWeight, fontStyle) { var targetColumnNames = ['last_name']; var targetWords = ['Doe']; var columnIndex = Array.from(document.querySelectorAll('table th')).findIndex(th => th.textContent.trim() === targetColumnNames[0]); if (columnIndex !== -1) { var rows = document.querySelectorAll('table tbody tr'); rows.forEach(function (row) { var cell = row.querySelector('td:nth-child(' + (columnIndex + 1) + ')'); var value = cell ? cell.textContent.trim() : ''; if (targetWords.includes(value)) { cell.style.setProperty('background-color', bgColor, 'important'); cell.style.setProperty('font-weight', fontWeight, 'important'); cell.style.setProperty('font-style', fontStyle, 'important'); } }); } else { console.error('Column not found: ' + targetColumnNames.join(', ')); } }highReportCell();

highReportCell('#6495ED', 'bold', 'italic');
